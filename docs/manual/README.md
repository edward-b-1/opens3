# Manual sources

`src/NN-*.md` are the chapters; `build.sh` compiles them into the single
file `docs/MANUAL.md`, which is what users read. Edit the chapters, run
`make manual` (or `docs/manual/build.sh`), and commit both.
